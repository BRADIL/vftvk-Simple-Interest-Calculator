# Car and Driver Reservation

This simple web application allows a small administration to book cars and drivers. It contains six predefined cars and six drivers. Users can select a car, choose a driver, pick a date and record the reservation. All logic is handled in the browser using vanilla JavaScript.

Open `index.html` in a browser to use the tool.
